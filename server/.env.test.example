# Test Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=gore_municipality_test

# JWT (test-only secret, never use this in production)
JWT_SECRET=change-this-to-a-secure-random-string
JWT_EXPIRES_IN=1h

# Email (test keys)
RESEND_API_KEY=re_test_key
RESEND_FROM_EMAIL=test@gore.gov.et
CONTACT_NOTIFICATION_EMAIL=admin@gore.gov.et
